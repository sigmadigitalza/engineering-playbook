name: Prepare Release

# Fires on every push to main. Computes the next semver from
# Conventional Commit subjects since the previous tag, regenerates the
# [Unreleased] section of CHANGELOG.md from merged PR titles, applies
# the version bump to deno.json, force-pushes a release/vX.Y.Z branch,
# and opens/updates a "Release vX.Y.Z" PR.
#
# Reusable workflow source: https://github.com/sigmadigitalza/rational-release

on:
  push:
    branches: [main]

permissions:
  contents: write
  pull-requests: write
  # rational-release@v1's reusable workflow declares `actions: write` (for its
  # optional post-push dispatch step). A reusable-workflow caller must grant at
  # least what the called workflow declares, or the run fails at startup — which
  # is what silently blocked every release since v0.3.0.
  actions: write

jobs:
  prepare:
    uses: sigmadigitalza/rational-release/.github/workflows/prepare-release.yml@v1
    with:
      # Treat `feat!:` as minor while we're still 0.x. Bumps to 1.0.0 are
      # opt-in via a manual major-bump commit later.
      pre1-cap: true
      # Pre-release gates: skills must be in sync, site must build.
      # Failure here means no release PR opens — gives the team a chance
      # to fix the underlying problem before the changelog is regenerated.
      pre-tasks: |
        deno task build:skills:check
        deno task build
      # After the version bump lands in deno.json, mirror it into the Claude
      # Code plugin manifest so the plugin's own version tracks each release,
      # then stage that file into the release commit. post-mirror-tasks runs
      # after the bump; pre-tasks would run one release behind.
      post-mirror-tasks: |
        deno task sync:plugin-version
      commit-paths: |
        plugins/sigma-engineering/.claude-plugin/plugin.json
